homeassistant:
  # Name of the location where Home Assistant is running
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: -6.1741
  longitude: 106.8296
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 0
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: Asia/Jakarta
  # Customization file
  customize: !include customize.yaml

  #auth-TOKEN-X
  auth_providers:
    - type: homeassistant

# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

# websocket api
websocket_api:

fastdotcom:
  scan_interval:
    minutes: 120

# Text to speech
tts:
  - platform: google_translate
    service_name: google_say

#xiaomi -==-==-==-
xiaomi_aqara:
  gateways:
  - mac: !secret xiaomi_aqara_mac1 
    key: !secret xiaomi_aqara_mac1_key
    
remote:
  - platform: xiaomi_miio
    name: "remote universal"
    host: !secret xiaomi_ir_remote1_ip
    token: !secret xiaomi_ir_remote1_token
    slot: 1
    timeout: 30
    hidden: false


# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
# http:
#   base_url: example.duckdns.org:8123

# Discover some devices automatically
#discovery:

# Sensors
sensor:
  # Weather prediction
  - platform: yr

# Text to speech
tts:
  - platform: google_translate

#Media Player -==-==-==-
media_player:

  - platform: kodi
    host: 192.168.1.11
    username: fanul
    password: hakimpalace100

  - platform: plex
    entity_namespace: 'plex'
    include_non_clients: false
    scan_interval: 5
    show_all_controls: true
    use_custom_entity_ids: true
    use_episode_art: true
    remove_unavailable_clients: true
    client_remove_interval: 600

#Google Assistant -==-==-==-
google_assistant:
  project_id: hakim-palace-11235813
  api_key: AIzaSyCorIacl3MqQIjxB4ZRp6efByZa7_TEyp0
  exposed_domains:
    - light
    - script
    - remote
    - media_player

#Google Home -==-==-==-
googlehome:
  devices:
    - host: 192.168.1.15

panel_iframe: !include panel_iframe.yaml
group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml
